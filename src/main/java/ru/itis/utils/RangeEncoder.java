package ru.itis.utils;

import ru.itis.structures.Fraction;
import ru.itis.structures.Node;

import java.math.BigInteger;
import java.util.*;

public class RangeEncoder {
//    public List<Integer> encode(List<Integer> intsList) {
//        Set<Integer> intSet = new HashSet<>(intsList);
//        BigInteger dictSize = new BigInteger(String.valueOf((intSet.size())));
//        int size = intsList.size();
//        Map<Integer, Fraction> p = new HashMap<>();
//        for (Integer integer : intSet) {
//            p.put(integer, Fraction.builder()
//                    .numerator((int) intsList.stream().filter(a -> a.equals(integer)).count())
//                    .denominator(size)
//                    .build());
//        }
//        BigInteger n = dictSize.pow(size);
//
//    }
}
